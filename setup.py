from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We're researching if the way crypto currienciemean variance efficient frontier',
    author='Thomas P',
    license='',
)
